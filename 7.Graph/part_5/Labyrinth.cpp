#include <bits/stdc++.h>
using namespace std;


int main() {

    int m, n, sr, sc, x, y;
    cin >> m >> n >> sr >> sc >> x >> y;

    // converting to 0-based indexing
    sr--; sc--;

    vector<string> grid(m);
    for (int i = 0; i < m; i++) cin >> grid[i];

    que
    return 0;
}