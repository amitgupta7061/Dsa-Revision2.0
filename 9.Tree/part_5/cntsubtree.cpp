#include <bits/stdc++.h>
using namespace std;


class Node {
  public:
    int data;
    Node* left;
    Node* right;
    Node(int val) {
        data = val;
        left = nullptr;
        right = nullptr;
    }
};

// Count substree having sum equal to given sum in a binary tree
class Solution {
public:
    int countSubtrees(Node* root, int x) {
        
    }
};

int main() {

    return 0;
}